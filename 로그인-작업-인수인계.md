# 로그인·찜 동기화 작업 (진행 중)

> 2026-09-11 시작. 구글플레이 출시 준비의 일부다.
> **작업이 끝나면 이 파일은 지운다** — 진행 상황을 적어 두는 임시 문서다.

## 지금 어디까지 왔나

코드는 다 붙였고 **Supabase 프로젝트만 만들면 동작한다.** 키가 없으면 로그인 기능이
통째로 꺼지도록 짜 두어서, 지금 배포돼 있어도 사용자에게 보이는 변화는 없다.

## 결정된 것

| 항목 | 결정 | 이유 |
|---|---|---|
| 1차 목표 | **구글플레이** | 애플은 2차. 소셜 로그인을 넣으면 App Store 4.8이 애플 로그인을 함께 요구한다 |
| 포장 | **TWA (Bubblewrap)** | 이미 PWA다. Capacitor는 iOS 갈 때 |
| 인증 | **Supabase Auth** | 구글·카카오·애플을 설정으로 붙인다. 직접 구현하면 애플 클라이언트 시크릿을 6개월마다 갱신해야 한다 |
| 장소 데이터 | **노션 그대로** | 운영진이 노션에서 직접 편집한다. 옮기면 관리자 콘솔을 만들어야 한다 |
| 로그인 | **선택제** | 로그인 없이도 지금처럼 전부 쓰인다. 파는 것은 "기기를 바꿔도 찜이 따라온다" 하나뿐 |

D1으로 먼저 만들었다가 Supabase로 갈아탔다. D1 코드·데이터베이스는 전부 지웠다.

## 지금 막혀 있는 것

**Supabase 무료 플랜은 계정당 활성 프로젝트 2개까지다.** 개인 계정(`wmf34a`)에는
`one-square` · `pin-money` 두 개가 이미 있어서 새로 만들 수 없다.

→ **육진대 구글 계정으로 Supabase에 새로 가입**해서 거기에 만들기로 했다.
소유권 측면에서도 그쪽이 맞다 — 개인 계정에 묶여 있으면 나중에 이관해야 한다.

## 다음에 할 일

1. **육진대 구글 계정으로 Supabase 가입** → New project
   - 이름 `yukjindae-map`, 리전 **Northeast Asia (Seoul)**, DB 비밀번호는 쓸 일이 없다
2. **SQL Editor에 `supabase/schema.sql` 붙여넣고 실행**
3. **Settings → API**에서 두 값 복사
   - `Project URL`
   - `anon public` 키
   - 둘 다 공개해도 되는 값이다. anon 키는 행 단위 보안(RLS)으로 보호된다
4. **Worker 시크릿 등록**
   ```
   npx wrangler secret put SUPABASE_URL
   npx wrangler secret put SUPABASE_ANON_KEY
   ```
   로컬 개발용으로는 `.dev.vars`에도 같은 두 줄을 넣는다
5. **Authentication → Providers**에서 Google·Kakao 켜고 각 콘솔에서 받은 키 입력
   - 두 콘솔에 등록할 리다이렉트 주소는 `https://<프로젝트ID>.supabase.co/auth/v1/callback`
   - 키 받는 절차: https://claude.ai/code/artifact/2663161c-2898-4cbb-a56b-3f9756d36d6c
6. **Authentication → URL Configuration**에 돌아올 주소를 넣는다
   - `https://yukjindae-map.wmf34a.workers.dev/**`
   - 나중에 커스텀 도메인을 달면 그것도 추가
7. 실제 로그인 → 찜 병합까지 확인

## 건드린 파일

| 파일 | 무엇을 |
|---|---|
| `supabase/schema.sql` | `favorites` 테이블 + RLS 3개 + 찜 500개 상한 트리거 + 노션 UUID 형식 검사 |
| `public/js/auth.js` | 로그인·로그아웃·찜 병합. Supabase 미설정이면 통째로 꺼진다 |
| `public/js/favorites.js` | `setFavorites()` 추가 — 로컬 저장 뒤 로그인 상태면 서버에도 반영 |
| `public/js/favorite-page.js` | 찜 화면 상단 로그인 칸 |
| `public/favorite.html` | 로그인 칸 자리 + `/naver-config` · `auth.js` 스크립트 |
| `public/css/style.css` | 로그인 칸 스타일 |
| `src/worker.js` | CSP에 Supabase·jsdelivr 허용, `__ENV__`로 공개 키 전달 |

## 설계에서 신경 쓴 것 — 고치기 전에 읽을 것

- **서버를 못 읽었을 때 로컬 찜을 지우지 않는다.** `fetchRemoteFavorites()`는 실패하면
  빈 배열이 아니라 `null`을 준다. "서버에 없다"와 "못 읽었다"를 같이 다루면
  네트워크가 끊긴 순간 찜이 날아간다.
- **병합은 어느 쪽도 지우지 않는다.** 로그인하면 서버 목록과 로컬 목록의 합집합이 된다.
  두 기기에서 각각 찜해 둔 사람이 양쪽에서 로그인하면 늘어나기만 해야 한다.
- **찜을 밀어 넣을 때 전부 지우고 다시 넣지 않는다.** 지울 것과 넣을 것만 추린다 —
  매번 다시 넣으면 `created_at`이 바뀌어 "최근 찜한 순"이 무너진다.
- **프론트가 Supabase를 직접 부른다.** Worker에 로그인 라우트가 없다.
  RLS가 유일한 방어선이므로 `schema.sql`의 정책을 끄면 anon 키를 가진 누구나 남의 찜을 읽는다.

## 겪은 함정

- **`favorite.html`만 `/naver-config`를 안 불러 `window.__ENV__`가 비어 있었다.**
  지도를 안 쓰는 페이지라 필요가 없었는데, 그 탓에 로그인 버튼이 영영 안 뜰 뻔했다.
  새 페이지에 로그인을 붙일 때 이 스크립트가 있는지 먼저 볼 것.
  (`scripts/build-ait.mjs`가 같은 형식을 치환하므로 미니앱도 함께 따라간다.)

## 남은 출시 과제 (로그인과 별개)

- **사진 출처 미확인 38곳** — 그중 대전 한밭수목원 곤충생태관은 워터마크가 있다고
  기록돼 있는데 아직 공개 중이다. 출시 전에 교체할 것
- 회원 탈퇴·계정 삭제 경로 (구글플레이 정책 필수)
- 개인정보처리방침 개정 — 지금 방침은 "회원가입이 없고 이름·이메일을 받지 않습니다"로
  시작한다. 로그인을 붙이는 순간 사실이 아니게 된다

## 관련 문서

- 구글플레이 입점 로드맵: https://claude.ai/code/artifact/9880767a-f93e-49d2-8a05-808d347dad85
- 출시 전 전수 점검: https://claude.ai/code/artifact/ff531afa-103b-4803-9ff1-7ed3738a7afb
- 현재 기능 명세: https://claude.ai/code/artifact/468dd02e-2a2d-4797-86c6-6f80e14c4d2a
- 사업자 명의 설명 자료: https://claude.ai/code/artifact/c6b3bf13-634c-4589-950b-e775099b2d69
