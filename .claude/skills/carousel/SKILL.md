---
name: carousel
description: 육진대 인스타 캐러셀·콘텐츠 작업. "캐러셀 만들어줘", "인스타 게시물", "홍보 이미지" 같은 요청에 쓴다.
---

# 캐러셀 작업은 이 저장소에 없다

2026-09-04 부터 `~/revision/yuk-carousel` 에서 한다. 앱 저장소와 수명이 다르고
렌더 산출물이 무거워서 옮겼다.

```bash
cd ~/revision/yuk-carousel
```

그 폴더의 `CLAUDE.md` · `SKILL.md` · `README.md` 를 읽고 따른다.
앱 화면을 캡처해야 하면 그 폴더의 `tools/serve.mjs` 가 이 저장소의 `public/` 을 서빙한다
(기본 경로가 `~/revision/yukjindae-map/public` 로 잡혀 있다).
